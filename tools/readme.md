## css-norm.html

正規化CSS的單HTML小工具。拉一個或兩個檔案進去，即可自動輸出轉換好的檔案到預設的下載資料夾，瀏覽器會自動處理檔名重複問題。

## diff-css.js

正規化後比對工具。
搭配已經設定的 VS Code build task 以快捷鍵 Ctrl+Shift+B 執行。

- 您在編輯器開著第一個 CSS 檔（例如 file1.css）。
- 按下快捷鍵，VS Code 會自動把第一個檔案帶入。
- 只會跳出一次提示視窗詢問「第二個檔案」。
- 會自動建立暫存資料夾及檔案。
- 自動叫出比對視窗。
